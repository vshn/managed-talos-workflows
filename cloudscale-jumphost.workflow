And I select the cluster to access
And I compile the cluster catalog
And I extract the cluster's base domain from the cluster inventory
And I extract the cloudscale API token from Vault
And I fetch the cluster's subnet from the cloudscale API
Then I set up an SSH jumphost in the cluster network
And I wait for the jumphost to become ready
And I show the user how to connect to the jumphost
